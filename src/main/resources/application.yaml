spring:
  application:
    name:
      ApplicationIncomeExpense
  web:
    resources:
      static-locations: classpath:/static/
    mvc:
      static-path-pattern: /**
  datasource:
    url: jdbc:postgresql://localhost:50177/postgres
    username: djavid
    password: djavid
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  server:
    servlet:
      context-path: /