spring:
  docker:
    compose:
      enabled: false
  application:
    name: ApplicationIncomeExpense
  datasource:
    url: jdbc:postgresql://localhost:5434/postgres
    username: djavid
    password: djavid
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration
  data:
    redis:
      host: localhost
      port: 6380
server:
  port: 8081

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE